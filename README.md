# Booking Lawyer - Сервис записи на консультации для юристов

Система онлайн-записи на консультации, аналог yClients, разработанная специально для юристов.

## Возможности

- 🔐 **Аутентификация**: Регистрация и вход в систему
- 📅 **Календарь доступности**: Юристы могут задать свое расписание (слоты по 30 или 60 минут)
- 🔗 **Уникальная ссылка**: Каждый юрист получает свою ссылку для записи
- 📝 **Бронирование**: Клиенты могут выбрать дату и время консультации
- 💳 **Оплата**: Кнопка оплаты (интеграция с платежной системой планируется позже)
- 👑 **Админ-панель**: Просмотр всех юристов и их статистики
- 📊 **Статистика**: Отслеживание количества записей и оплат

## Технологический стек

- **Frontend**: Next.js 14, React, TypeScript, Tailwind CSS
- **Backend**: Next.js API Routes
- **Database**: PostgreSQL + Prisma ORM
- **Authentication**: NextAuth.js
- **Date handling**: date-fns

## Деплой на Railway 🚂

Для развертывания на Railway следуйте инструкциям в [DEPLOYMENT.md](./DEPLOYMENT.md)

**Быстрый старт:**
1. Создайте проект на [Railway.app](https://railway.app)
2. Подключите GitHub репозиторий
3. Добавьте PostgreSQL базу данных
4. Настройте переменные окружения (NEXTAUTH_URL, NEXTAUTH_SECRET)
5. Примените схему БД: `railway run npx prisma db push`
6. Создайте админа: `railway run npm run seed`

Подробная инструкция в [DEPLOYMENT.md](./DEPLOYMENT.md)

## Локальная установка и запуск

### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd booking_lawyer
```

### 2. Установите зависимости

```bash
npm install
```

### 3. Настройте базу данных

Создайте PostgreSQL базу данных и обновите файл `.env`:

```env
DATABASE_URL="postgresql://user:password@localhost:5432/booking_lawyer?schema=public"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-change-this-in-production"
```

### 4. Примените миграции базы данных

```bash
npx prisma generate
npx prisma db push
```

### 5. Заполните базу данных тестовыми данными

```bash
npm run seed
```

Это создаст:
- Админа: `admin@example.com` / `admin123`
- Тестового юриста: `lawyer@example.com` / `lawyer123`

### 6. Запустите проект

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Структура проекта

```
booking_lawyer/
├── app/
│   ├── api/              # API эндпоинты
│   │   ├── auth/         # NextAuth
│   │   ├── register/     # Регистрация
│   │   ├── availability/ # Управление доступностью
│   │   └── bookings/     # Управление записями
│   ├── login/            # Страница входа
│   ├── register/         # Страница регистрации
│   ├── dashboard/        # Личный кабинет юриста
│   ├── admin/            # Админ-панель
│   └── book/[link]/      # Публичная страница записи
├── components/           # React компоненты
├── lib/                  # Утилиты (Prisma, auth)
├── prisma/
│   ├── schema.prisma     # Схема базы данных
│   └── seed.ts           # Seed скрипт
└── types/                # TypeScript типы
```

## Использование

### Для юристов

1. Зарегистрируйтесь на `/register`
2. Войдите в систему на `/login`
3. В личном кабинете:
   - Настройте свое расписание (добавьте слоты доступности)
   - Скопируйте свою уникальную ссылку для записи
   - Просматривайте список записей клиентов

### Для клиентов

1. Перейдите по ссылке юриста (например, `/book/abc123xyz`)
2. Выберите удобную дату
3. Выберите свободное время
4. Заполните контактные данные
5. Нажмите "Продолжить к оплате"

### Для администратора

1. Войдите как админ (`admin@example.com` / `admin123`)
2. В админ-панели доступны:
   - Список всех юристов
   - Статистика по записям
   - Информация об оплатах
   - Записи каждого юриста

## Конфигурация

### Изменить длительность слотов

В схеме базы данных поддерживается любая длительность, но в UI по умолчанию доступны 30 и 60 минут. Вы можете изменить это в файле `components/DashboardClient.tsx`.

### Настройка расписания

Юристы могут создавать несколько слотов доступности для каждого дня недели с разной длительностью.

## Планы развития

- [ ] Интеграция с платежными системами (Stripe, PayPal, Yookassa)
- [ ] Email уведомления
- [ ] SMS напоминания
- [ ] Видеоконференция для онлайн-консультаций
- [ ] Экспорт данных в Excel/PDF
- [ ] Календарь в формате месяца
- [ ] Возможность отмены/переноса записей
- [ ] Отзывы и рейтинги юристов

## База данных

### Модели

- **User**: Пользователи системы (юристы и админы)
- **Availability**: Слоты доступности юристов
- **Booking**: Записи клиентов на консультации

### Миграции

Для создания новых миграций:

```bash
npx prisma migrate dev --name migration_name
```

## Скрипты

### Разработка
- `npm run dev` - Запуск в режиме разработки
- `npm run build` - Сборка для продакшена
- `npm start` - Запуск продакшен сборки
- `npm run lint` - Проверка кода

### База данных
- `npm run seed` - Заполнение базы тестовыми данными
- `npm run db:push` - Применить схему Prisma к базе данных
- `npm run db:studio` - Открыть GUI для базы данных
- `npm run prisma:generate` - Сгенерировать Prisma Client

### Создание администратора
```bash
npx tsx scripts/create-admin.ts <email> <password> [name]

# Пример:
npx tsx scripts/create-admin.ts admin@example.com SecurePass123 "Admin"
```

## Лицензия

MIT

## Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории.
